# Release gate

No merge, release tag, or submission push is authorized from this validation branch until the exact head has a successful GitHub Actions run and the repeated benchmark outputs compare byte-for-byte.
