# Reference–kernel equivalence validation trigger

This marker records that the independent reference oracle and equivalence tests were added to tag4. CI must execute before this gate is marked complete.
