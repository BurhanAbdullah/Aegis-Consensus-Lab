# tag4 CI trigger marker

This marker intentionally triggers the tag4 validation workflow after the import-path correction. It must be removed or retained only as a validation audit marker after the run is inspected.
